apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parsops-chart.fullname" . }}-test-connection"
  labels:
    {{- include "parsops-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parsops-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
